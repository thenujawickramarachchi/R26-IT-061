import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF5B4BFF);

  // Background
  static const background = Color(0xFFF5F7FB);
  static const card = Colors.white;

  // Text
  static const title = Color(0xFF172033);
  static const subtitle = Color(0xFF8A94A6);
  static const body = Color(0xFF5F6B7A);

  // Risk
  static const highRisk = Color(0xFFFF2F6D);
  static const mediumRisk = Color(0xFFFFA500);
  static const lowRisk = Color(0xFF228B22);

  // Others
  static const lightPrimary = Color(0xFFEFF4FF);
  static const lightPink = Color(0xFFFFEEF4);
  static const lightGray = Color(0xFFF2F5FA);
}