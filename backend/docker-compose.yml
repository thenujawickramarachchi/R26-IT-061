services:
  dengue-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      MODEL_ROOT: /models
      MODEL_DEVICE: cpu
    volumes:
      - ../Dengue_NLP_Model:/models:ro
    restart: unless-stopped
